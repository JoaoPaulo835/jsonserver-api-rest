import React from "react";
import ReactDOM from "react-dom";
import App from "./App";
//essa parte do código é a entrada da aplicação
//é aqui que se inicia a aplicação, o ponto de partida. 

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
